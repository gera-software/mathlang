#include <criterion/criterion.h>
#include "repl.h"

// Test(repl_suite, test_repl_start_does_not_crash) {
//     // For now, just call the function; if it doesn't segfault, it passes.
//     repl_start();
//     cr_assert(true);
// }